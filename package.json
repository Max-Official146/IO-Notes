{
  "name": "smartnotes-ai-workspace",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace smartnotes-api",
    "dev:web": "npm run dev --workspace smartnotes-web",
    "build:web": "npm run build --workspace smartnotes-web",
    "start:api": "npm run start --workspace smartnotes-api"
  }
}
